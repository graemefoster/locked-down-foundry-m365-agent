name: Publish teams-agent to Teams / M365
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    uses: ./.github/workflows/publish-teams.yml
    with:
      agent-directory: agents/grf-2026-teams-agent
