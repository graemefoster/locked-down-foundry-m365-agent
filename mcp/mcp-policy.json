{
  "description": "Per-server, per-agent rate limits for MCP calls through the APIM AI gateway. Agent names are resolved to live instance_identity.client_id values by scripts/apply-mcp-policy.ps1 on the in-VNet runner. Unresolved agents are excluded. Provisioning applies deny-all until the governance workflow resolves and applies this policy.",
  "renewalPeriodSeconds": 60,
  "servers": [
    {
      "name": "mcp",
      "agents": [
        {
          "name": "teams-agent",
          "requestsPerMinute": 50000
        }
      ]
    }
  ]
}
